/** Loader companion for product IM presentation; domain services live in the runtime and API. */
export const name = 'gestaltrun-ui-im'

/** The Host companion contributes no runtime state. */
export function apply(): void {}
